# แก้ไขปัญหา API เรียก localhost แทน https://getkunlun.me/api

## สรุปปัญหา
เว็บไซต์ที่ https://getkunlun.me ยังคงเรียก API ไปยัง localhost:3001 แทนที่จะเรียกไปยัง https://getkunlun.me/api

## การแก้ไขที่ทำแล้ว

### 1. อัปเดต `src/services/apiClient.ts`
เปลี่ยนจาก:
```typescript
const API_BASE_URL = '/api';
```

เป็น:
```typescript
const API_BASE_URL = window.location.hostname.includes('getkunlun.me') 
  ? 'https://getkunlun.me/api' 
  : '/api';
```

ไฟล์นี้แก้ไขเรียบร้อยแล้วและ commit ไปยัง Git แล้ว

## วิธีแก้ไขบนเซิร์ฟเวอร์

### ขั้นตอนที่ 1: Pull โค้ดล่าสุด
```bash
cd /path/to/your/project
git pull origin main
```

### ขั้นตอนที่ 2: ติดตั้ง Dependencies
```bash
npm install
```

### ขั้นตอนที่ 3: Build โปรเจค
```bash
npm run build
```

### ขั้นตอนที่ 4: Deploy ไฟล์ที่ Build แล้ว
หลังจาก build สำเร็จ ไฟล์จะอยู่ในโฟลเดอร์ `dist/`:
- `dist/index.html`
- `dist/app-[timestamp].js`
- `dist/style-[timestamp].css`

คัดลอกไฟล์เหล่านี้ไปยังโฟลเดอร์ root ของเว็บไซต์:
```bash
cp dist/* ./
```

### ขั้นตอนที่ 5: Restart API Server
```bash
cd server
node server.js
```

หรือถ้าใช้ PM2:
```bash
pm2 restart server
```

## การตรวจสอบว่าแก้ไขสำเร็จ

1. เปิดเว็บไซต์ https://getkunlun.me
2. กด F12 เปิด Developer Tools
3. ไปที่ Tab "Console"
4. ดูข้อความ:
   ```
   🔗 API Base URL: https://getkunlun.me/api
   🌐 Current host: getkunlun.me
   ```

5. ไปที่ Tab "Network"
6. ลองทำการ Login
7. ตรวจสอบว่า Request URL ควรเป็น `https://getkunlun.me/api/login` ไม่ใช่ localhost

## สำรอง: วิธีแก้ไขด่วนถ้า Build ไม่ได้

ถ้า build มีปัญหา สามารถแก้ไขไฟล์ที่ build แล้วโดยตรง:

1. หาไฟล์ `app-[timestamp].js` ที่กำลังใช้งานอยู่ (ดูจาก `index.html`)
2. เปิดไฟล์และค้นหา: `"http://localhost:3001/api"`
3. แทนที่ด้วย: `"/api"`
4. บันทึกไฟล์

⚠️ **คำเตือน**: วิธีนี้เป็นการแก้ไขชั่วคราว ทุกครั้งที่ build ใหม่จะต้องทำซ้ำ

## ไฟล์ที่ต้องลบ (ไฟล์เก่าที่มี localhost)

ไฟล์เหล่านี้เป็น built files เก่าที่ยังมี localhost อยู่ ควรลบทิ้ง:
- `app-1771086136583.js`
- `app-1771086378282.js`  
- `app-1771086466341.js`
- `app-1771086500613.js`
- `style-1771086136583.css`
- `style-1771086378282.css`
- `style-1771086466341.css`
- `style-1771086500613.css`
- โฟลเดอร์ `assets/` (ถ้ามี)

## การทดสอบในเครื่อง Local

ถ้าต้องการทดสอบว่าการแก้ไขใช้งานได้:

1. เปิด `src/services/apiClient.ts`
2. แก้ `getkunlun.me` เป็น `localhost` เพื่อทดสอบ:
```typescript
const API_BASE_URL = window.location.hostname.includes('localhost') 
  ? 'http://localhost:3001/api' 
  : '/api';
```
3. Build และทดสอบ
4. เปลี่ยนกลับเป็น `getkunlun.me` ก่อน deploy

## สาเหตุของปัญหา

ปัญหาเกิดจาก:
1. Source code เดิมใช้ relative URL (`/api`) ซึ่งถูก Vite proxy ไปยัง `localhost:3001` ใน development
2. เมื่อ build, บางส่วนของโค้ดยังคงมี hardcoded `localhost:3001/api`
3. ไฟล์ที่ build ออกมา (JavaScript bundles) ถูก cache ไว้

## การป้องกันปัญหาในอนาคต

เพื่อป้องกันปัญหานี้ในอนาคต:
1. ก่อน deploy ให้ลบไฟล์ build เก่าทั้งหมด
2. ใช้ environment variables สำหรับ API URL
3. ตรวจสอบ Network tab ทุกครั้งหลัง deploy
4. ใช้ CI/CD pipeline ที่ build ใหม่ทุกครั้ง

## ติดต่อ Support

หากยังมีปัญหา กรุณาตรวจสอบ:
- ไฟล์ `index.html` ว่ากำลัง load JavaScript ไฟล์ไหน
- Developer Console มี error อะไรบ้าง
- Network tab แสดง Request URL เป็นอะไร
